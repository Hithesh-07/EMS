{
  "name": "employee-management-system-root",
  "version": "1.0.0",
  "description": "Unified runner for Employee Management System",
  "scripts": {
    "start": "npx concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "npm run start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
